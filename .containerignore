# Never copied into the build context.
node_modules
dist
.astro

# public/media/ is the ARCHIVE now — Oscar's actual photographs — so it must be copied in.
# It previously held only generated placeholders, which is why it was excluded. The
# DERIVATIVES stay out: those are rebuilt inside the image so the colour assertion runs
# against files this build produced, not files a laptop produced.
public/media/derived
public/media/social

# Generated from Caddyfile.template by scripts/gen-csp.mjs during the build. Copying a
# host copy in would ship stale CSP hashes, which blocks the site's own scripts.
Caddyfile

.git
.gitignore
.github
*.log
npm-debug.log*
.DS_Store
.vscode
.idea
README.md
